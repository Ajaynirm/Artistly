import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: [
      "media.istockphoto.com",
      "t3.ftcdn.net",
      "beyonddance-lynnfield.com",
      "img.freepik.com",
      "images.unsplash.com",
    ],
  },
};

export default nextConfig;
